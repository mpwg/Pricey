# ============================================================================
# OCR Service Local Overrides (Optional)
# ============================================================================
# This file is for LOCAL OVERRIDES ONLY. All shared configuration is in the
# root .env.local file (../../.env.local).
#
# Copy this to .env.local if you need to override specific settings for
# OCR service development without affecting other services.
# ============================================================================

# Uncomment to use Mac's accelerated Ollama (10-20x faster!)
# brew install ollama && ollama serve --host 0.0.0.0:10000
# LLM_BASE_URL=http://localhost:10000

# Uncomment to use a different vision model
# LLM_MODEL=llava
# LLM_MODEL=llama3.2-vision:11b
# LLM_MODEL=moondream

# Uncomment to adjust OCR processing limits
# OCR_CONCURRENCY=10
# OCR_TIMEOUT=60000
